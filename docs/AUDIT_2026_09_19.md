# GitHub-main audit, 19 September 2026

Authoritative starting commit:
`a7b6027a491dc0e4e07f82ccab6de845b131ad21` (GitHub `main`).

## What is validated and retained

- The pinned Python environment installs on Python 3.13.5.
- The complete pre-change suite passes: 156 tests, with three non-failing warnings.
- The checkout contains 47 converged primary run summaries, 15 pilot runs, 35
  rotated-digit runs, 39 reduced-resolution continuation runs, 20 trained-circle
  quotient runs, and 40 rational trained-circle layer certificates.
- The exact restricted certificates, compiled-Lean logs, rotated-digit replay,
  and previously committed result artifacts were not replaced or regenerated.

## Artifact-backed gaps

After adding the frozen nuisance-shift study to the v2 completion gate,
`scripts/freeze_results.py --check-only` reports `ready: false` and 68 problems.
The live scan finds:

| study | expected | currently accounted by live scan |
|---|---:|---:|
| main | 130 | 56 (including reusable pilot configurations) |
| width | 175 | 19 |
| depth | 140 | 19 |
| relevance | 210 | 19 |
| nuisance-shift training | 140 | 19 |
| cylinder | 35 | 0 |
| swapped | 35 | 0 |
| small network | 120 | 0 |
| dSprites | 35 | 0 |
| CIFAR-10 | 35 | 0 |
| CIFAR-100 | 35 | 0 |

Full production-trajectory metrics are missing for every primary/pilot run in
the checkout. Nuisance-shift evaluations are absent. Rotated digits are the only
complete committed image study.

## Stale records

`results/completion/current_inventory.json` was generated in a different recovered
workspace and refers to paths not present on GitHub `main`.  It is retained as a
historical recovery record but must not be used as current evidence.  Likewise,
`EXECUTION_STATE.json` contains old process IDs and is not evidence of live work.
The dated live readiness artifact is
`results/completion/github_main_readiness_2026_09_19.json`.

## Consequence for execution

No completed result is recomputed.  New work uses separate output directories,
and the production freeze remains closed until the live scanner passes.  The
scientific framing, theory, exact controls, and prospective protocol are completed
before any missing high-cost training or full-resolution metrics are launched.
